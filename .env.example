# Supabase — find these in your Supabase dashboard under Project Settings > API
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_ANON_KEY=

# Anthropic API key — used server-side only in Supabase Edge Functions
# Set via: supabase secrets set ANTHROPIC_API_KEY=sk-ant-...
# Do NOT add to .env.local — it must never reach the browser
# ANTHROPIC_API_KEY=
